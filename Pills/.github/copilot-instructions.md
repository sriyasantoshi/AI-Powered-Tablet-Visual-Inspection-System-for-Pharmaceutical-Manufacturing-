# Image Browser Application

This is a Node.js/Express application for browsing and uploading images from a local watcher folder.

## Features
- View images from the watcher folder
- Upload new images
- Image gallery display
- Responsive UI

## Setup
Run `npm install` to install dependencies, then `npm start` to run the server on localhost:3000.
